# プロジェクト概要
Spring Boot, MyBatis, Thymeleafを利用した「在庫・レシピ管理システム」。原料・添加物・製品のマスタ管理、在庫の入出庫（FIFO）、および加水率や産地指定を伴うレシピの登録・管理を行う。

# 技術スタック
- Backend: Java (Spring Boot), MyBatis (XMLマッピング)
- Frontend: Thymeleaf, Vanilla JS, CSS
- DB設計方針: 論理削除（is_active, is_deleted）を採用（在庫履歴を除く）。

# コア機能とビジネスロジック

## 1. マスタ管理 (Item / Material)
- **Item (製品)**: バッチ製造数、標準原価、販売価格、加水率（min/max）を持つ。
- **Material (原料・添加物)**: 単位、粉体フラグ(`isPowder`)、タイプ(`RAW` / `ADDITIVE`)を持つ。

## 2. 在庫管理 (Inventory)
- **Stock (在庫状態)**: `inventory_stocks`。`quantity` (実在庫) と `reserved_quantity` (製造中の仮消費ロック) を持つ。有効在庫は `quantity - reserved_quantity`。
- **Transaction (履歴)**: `inventory_transactions`。入出庫の履歴を全て記録。
- **入荷ロジック (IN)**: `material_id`, `lot_number`, `origin` が完全一致する在庫が存在すれば `quantity` を加算 (UPSERT)。新規ならINSERT。同時にTransactionに「IN」履歴を記録。
- **消費ロジック (FIFO)**: 製造時の在庫消費は、賞味期限(昇順) → 入荷日(昇順) の順で行う。複数ロットを跨ぐ消費も自動計算し、Transactionに「PRODUCTION」として履歴を残す。

## 3. レシピ管理 (Recipe)
- **構成**: 1つのItemに対し、複数のRecipe（行）が紐づく。
- **産地制限**: `recipe_origins`テーブル。特定の原料に対して「使用可能産地 (`allowedOrigins`)」を制限できる。未設定（空）の場合は「産地不問」。
- **加水率計算ロジック**: 粉体原料(`isPowder=true`)の合計重量を基に、加水率(%)と加水量(ml)をフロントエンド(JS)でリアルタイム相互計算する。

# 主要ファイル構成
- **Controller**: `HomeController`, `ItemController`, `RawController`, `AdditiveController`, `InventoryController`, `RecipeController`
- **Service/Impl**: 各機能のビジネスロジックを担当。トランザクション管理(`@Transactional`)はService層で担保。
- **Mapper/XML**: CRUD操作。特に `InventoryStockMapper.xml` のUPSERTやFIFO取得クエリが重要。
- **DTO (Form/Data)**: 入力値の受け取りとバリデーション。`RecipeRegisterData` で複数行のリスト送信を処理。
- **Frontend**: 
  - `common.js`: モーダル制御(`openConfirmModal`)
  - `recipe-register.js`: 行の動的追加、加水率のリアルタイム計算、産地チェックボックスの動的生成。
  - `operation.html`: 入荷登録画面（原料・添加物のグループ化セレクトボックス）。