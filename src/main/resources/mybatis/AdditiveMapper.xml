<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.app.mapper.AdditiveMapper">
	
    <select id="findAll" resultType="Additive">
        SELECT id, name, unit, is_deleted 
        FROM additives 
        WHERE is_deleted = 0
    </select>
    
    <select id="findDeletedAdditives" resultType="Additive">
		SELECT id, name, unit, is_deleted 
        FROM additives 
        WHERE is_deleted = 1
	</select>

	<select id="findById" resultType="Additive">
		SELECT id, name, unit, is_deleted 
		FROM additives 
		WHERE id = #{id}
	</select>
    
    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO additives (name, unit)
        VALUES (#{name}, #{unit})
    </insert>
    
    <update id="update">
        UPDATE additives 
        SET name = #{name}, 
        unit = #{unit} 
        WHERE id = #{id}
    </update>
	
	<update id="logicalDelete">
		UPDATE additives 
		SET is_deleted = 1
		WHERE id = #{id}
	</update>
	
	<update id="restore">
		UPDATE additives
		SET is_deleted = 0 
		WHERE id = #{id}
	</update>
	
</mapper>